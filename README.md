# Runtime-Platform-Go
Practical runtime platform clone using go

# How to start
clone repo, then run on terminal  
`go run ./cmd/control-plane/Server.go`

then `go run ./cmd/runtime-agent/agent.go`

then `go run ./cmd/Job-creator/AgentJob.go`

